## D-20260917-1 visual direction
- context: owner supplied two Pinterest dashboard references and asked for Shopee colors
- options: shadcn default neutral look | copy references with blue | copy references with Shopee orange palette
- chosen: copy reference layout/components; single brand hue Shopee orange #EE4D2D with tint scale; tokens in docs/plan/phase-1-ui-demo.md
- because: owner explicit; one hue keeps ads data readable and ties to Shopee
- revisit-when: owner rejects first screen build
- touches: [src/index.css, tailwind config, docs/plan/refs/]

## D-20260917-2 shadcn-first frontend workers
- context: owner wants workers to check the shadcn registry before writing UI
- options: one worker with a conditional shadcn section | separate frontend-worker agent selected by unit.kind | shadcn check only in audit
- chosen: separate frontend-worker agent; graph-engineer tags unit.kind; auditor review lens runs shadcn audit checklist for frontend units
- because: backend workers do not carry shadcn instructions (fewer tokens); enforcement at both build and audit
- revisit-when: a unit mixes frontend and backend and the split hurts
- touches: [.claude/agents/frontend-worker.md, .claude/agents/graph-engineer.md, .claude/agents/auditor.md, .claude/workflows/build.js]

## D-20260917-3 read-only v1
- context: whether the app may write to Shopee (pause, budget, apply recommendation)
- options: read-only advice | read+write with confirmation | read now, write later
- chosen: read-only; recommendations are step-by-step instructions for Seller Centre
- because: faster release, lower risk, easier API scope approval
- revisit-when: owner requests "Terapkan" button; requires write scope + audit log
- touches: [src/features/products, supabase/functions]

## D-20260917-4 ad types in scope
- context: Shopee has Product Ads, Shop Ads, Live/Affiliate
- options: Product only | Product + Shop | all three
- chosen: Product Ads (Search + Discovery) + Shop Ads; Live/Affiliate backlog
- because: covers most seller spend; AMS uses a different API
- revisit-when: sellers ask for affiliate data
- touches: [src/mocks/, src/api/, docs/plan/features.md]

## D-20260917-5 v1 extras
- context: which non-core features enter the roadmap
- options: none | alerts | weekly report | team roles
- chosen: ROAS-drop alerts (WhatsApp + email) and weekly PDF/email report in phase 4; team roles backlog
- because: both increase retention and need only read data
- revisit-when: tenants request multi-user access
- touches: [supabase/functions, docs/plan/features.md]

## D-20260917-6 billing provider
- context: SaaS payments for Indonesian sellers
- options: Midtrans/Xendit | Stripe | manual activation
- chosen: Midtrans or Xendit (final pick at phase 2 start); QRIS, VA, e-wallet
- because: local payment methods; Stripe is card-only for Indonesia
- revisit-when: phase 2 start, compare fees and Supabase webhook fit
- touches: [supabase/functions/billing, docs/plan/features.md]

## D-20260917-7 shadcn kbd component during Layout build
- context: tokens-shell worker needed a keyboard-shortcut hint element in the top bar's CMD-K search affordance
- options: hand-roll a <kbd> element | install shadcn's registry kbd component
- chosen: install shadcn kbd (npx shadcn add @shadcn/kbd) -> src/components/ui/kbd.tsx
- because: reuses the project's shadcn-first convention instead of one-off markup, matches existing component library
- revisit-when: never (component now part of shared ui/)
- touches: [src/components/ui/kbd.tsx, src/app/Layout.tsx]

## D-20260917-8 attention-rule test coverage deferred
- context: api-format-layer audit flagged useAttention's median+ROAS filter (F1.4) as non-trivial logic verified only ad hoc, no persisted test
- options: add src/api/ads.test.ts now, expanding this unit's file scope | ship without a test and track as open issue
- chosen: ship without a test, track in docs/logs issues-open
- because: unit's file list didn't include a test file; adding one is a small isolated follow-up, not worth reopening the unit mid-build
- revisit-when: useAttention logic changes again, or before phase 4 Shopee API swap
- touches: [src/api/ads.ts]

## D-20260917-9 hourly/best-hours self-check re-implements logic instead of exercising real hooks
- context: useBestHours/useBudget are TanStack Query hooks; the api unit's tsx self-check can't easily run a hook outside React, so it re-derives the same math standalone
- options: re-implement the math in a standalone tsx check (fast, disjoint from React runtime) | stand up a QueryClientProvider harness to call the real hooks | export computeBestHours/computeBudget as plain functions for the check to call directly
- chosen: standalone re-implementation for now; ship, flag as open issue
- because: unit scope was time-boxed to tsc+tsx-assert per the task contract; a harness or refactor is a larger diff than the unit called for
- revisit-when: computeBestHours/computeBudget logic changes again, or before this becomes user-facing in phase 4
- touches: [src/api/ads.ts]

## D-20260917-10 Ringkasan widgets built without wiring into page
- context: 7 units built new/edited Ringkasan widgets in parallel; the compose unit (edit RingkasanPage.tsx) was never dispatched/run in this batch
- options: ship widgets unwired and flag it | block the whole task until compose runs | fabricate a compose result
- chosen: ship widgets unwired and flag it
- because: each widget passed tsc -b/build in isolation and audits passed; wiring is a distinct, well-specified follow-up and no compose report exists to synthesize honestly
- revisit-when: compose unit runs and RingkasanPage.tsx is updated
- touches: [src/features/ringkasan/RingkasanPage.tsx]

## D-20260917-10 workflow unit cap
- context: 7-unit plan silently lost its compose unit to slice(0, 6)
- options: keep 6 and require compose inline | raise to 8 with logged warning | no cap
- chosen: cap 8, log() a WARNING naming dropped units; graph-engineer told to merge rather than exceed
- because: silent truncation reads as success; 8 covers screen-sized tasks
- revisit-when: a task legitimately needs >8 disjoint units
- touches: [.claude/workflows/build.js, .claude/agents/graph-engineer.md]

## D-20260917-11 chart animations off
- context: Recharts 3 Area/Pie entry animation did not run on first paint in headless capture; charts looked empty
- options: keep animation | disable on dashboard charts | delay screenshots
- chosen: isAnimationActive=false on RoasSpendChart areas and SpendByTypeDonut pie
- because: deterministic first paint for demos and screenshots; no visual loss on a data dashboard
- revisit-when: owner wants motion on charts
- touches: [src/features/ringkasan/RoasSpendChart.tsx, src/features/ringkasan/SpendByTypeDonut.tsx]

## D-20260917-12 dashboard grid discipline
- context: owner rejected the mixed 5/7, 5/4/3, 8/4, 7/5 layout as messy
- options: keep widgets, tune widths | strict rows (8/4, 12, 4/4/4) and merge widgets | drop widgets to 4
- chosen: strict rows; KPI strip carries 8 metrics in 4 cards; donut removed in favor of allocation bars inside the budget card; heatmap full width; three equal list cards
- because: uniform row heights and fewer cards read as production; no data lost
- revisit-when: owner asks for the donut or a second KPI row back
- touches: [src/features/ringkasan/, docs/plan/phase-1-ui-demo.md]
- supersedes: none (D-20260917-7 screen set unchanged)

## D-20260917-13 fluid visual system
- context: owner supplied ref-3 (borderless cards, pill controls, navy dark) and asked for core components to change
- options: per-widget class overrides | edit shadcn core files + tokens | fork a second component set
- chosen: edit tokens in src/index.css and the shadcn core files (card, button, input, toggle, toggle-group, select, popover, skeleton, empty, sidebar); widgets untouched
- because: one place per primitive; every current and future screen inherits the look
- revisit-when: re-running shadcn add with overwrite on an edited primitive; diff before accepting
- touches: [src/index.css, src/components/ui/, src/app/Layout.tsx]
- supersedes: D-20260917-1 layout bullets on card border/shadow (brand palette unchanged)

## D-20260917-14 splash and loading strategy
- context: mock data resolved instantly so no loading UI was ever visible; owner asked for loading state and a fluid splash
- options: React splash component | static splash in index.html removed after mount | no splash, skeletons only
- chosen: static splash in index.html (shows before the JS bundle, dark-aware) + main.tsx fade with 700ms minimum; route chunks lazy with PageSkeleton; header loading bar from useIsFetching; mock latency 450-850ms
- because: splash covers bundle load for real users, skeletons cover data, bar covers refetches; all three are real production paths, latency is the only mock
- revisit-when: phase 4 live data (drop withLatency) or owner finds 700ms too long
- touches: [index.html, src/main.tsx, src/app/, src/api/ads.ts]

## D-20260917-9 Keep speculative thumbnail field in ShopeeAdCampaign mock shape
- context: mock unit added ShopeeAdCampaign.thumbnail (bg/initials) with no current UI consumer, diverging from the "mirror the API shape" mock rule.
- options: drop the field until a table/UI renders it | keep it documented as a deliberate UI-only placeholder
- chosen: keep it documented as a deliberate UI-only placeholder
- because: low severity, cheapest fix per audit; removing it risks another round-trip when the ads table UI lands next
- revisit-when: catalogue/ads-table UI unit lands without using thumbnail, or a stricter mock-shape rule is enforced
- touches: [src/mocks/shopee-ads.ts]

## D-20260917-10 usePerformance and useHourlySeries stay separate rounding paths for itemsSold
- context: both derive itemsSold from orders*1.15, but usePerformance rounds once on the range total while useHourlySeries rounds per hour, causing minor sum drift.
- options: extract a shared unrounded-then-round-once helper | leave as-is with a ponytail note
- chosen: leave as-is with a ponytail note (issue logged as open, not fixed)
- because: drift is cosmetic (derived/fabricated mock metric), not worth a shared abstraction at this phase
- revisit-when: UI displays both totals side by side and the drift becomes visible/reported
- touches: [src/api/ads.ts]

## D-20260917-11 ROAS card formatting uses a local formatter, not lib/format's formatRoas
- context: ref-5-shopee-ads-performa.png shows ROAS as "15,12" (2 decimals, no unit), but the existing lib/format.ts formatRoas and docs/plan/phase-1-ui-demo.md:113 both use an "x" suffix ("3,2x"); the performance-cards worker added a new local Intl formatter to match the screenshot without touching the shared helper or writing this block.
- options: reuse/extend the existing formatRoas (change its output globally) | add a new local formatter scoped to PerformanceCards | add a second exported formatter in lib/format.ts for this surface
- chosen: local formatter scoped to PerformanceCards.tsx (as the worker shipped it)
- because: changing formatRoas globally risks breaking any other caller expecting the "x" suffix; a scoped formatter is the smaller diff and matches the reference screenshot exactly, which is the acceptance source of truth for this task
- revisit-when: another surface needs the same "no-x, 2-decimal" ROAS format — fold both call sites into a shared exported formatter in lib/format.ts then
- touches: [src/features/ringkasan/PerformanceCards.tsx, src/lib/format.ts, docs/plan/phase-1-ui-demo.md]

## D-20260917-15 ringkasan mirrors shopee ads manager
- context: owner supplied Shopee Seller Centre ads screenshots (ref-4, ref-5, ref-6) and asked the dashboard content to match
- options: keep generic dashboard widgets | mirror Shopee Ads Manager (Performa 8 cards + hourly chart + Daftar Iklan Produk) | hybrid
- chosen: mirror Shopee Ads Manager content inside our fluid visual system; six generic widgets deleted
- because: sellers already know this layout; zero learning curve for the demo
- revisit-when: catalogue reviewer needs a home on Ringkasan
- touches: [src/features/ringkasan/, src/api/ads.ts, src/mocks/shopee-ads.ts]
- supersedes: D-20260917-12

## D-20260917-16 mock history 60 days
- context: 30-day default range had no previous period, every delta read 0,00%
- options: null deltas shown as "-" | extend mock to 60 days | shorten default range to 7 days
- chosen: DAYS = 60 in the mock
- because: deltas are a core part of the Shopee look; real API will have history anyway
- revisit-when: phase 4 live data
- touches: [src/mocks/shopee-ads.ts]

## D-20260917-17 Ad-list tab counts omitted
- context: Spec allowed showing counts on Semua/Perlu Tindakan/Sehat tab pills only "if cheap"; a second useAdList query per tab would add fetches.
- options: fetch extra useAdList calls for counts | omit counts entirely
- chosen: omit counts entirely
- because: avoids extra network calls for a cosmetic badge; matches YAGNI framing in the task
- revisit-when: owner asks for tab counts explicitly
- touches: [src/features/ringkasan/AdList.tsx]

## D-20260917-18 Ad-list Diagnosis filter dropped from UI but not from API
- context: Task requires dropping the Diagnosis select from AdList UI; useAdList/AdListParams still declare a required diagnosis field.
- options: make diagnosis optional in AdListParams | hardcode diagnosis:'all' at the only call site
- chosen: hardcode diagnosis:'all' at the only call site
- because: smallest diff; changing the shared hook's param shape is out of scope for a UI-only unit
- revisit-when: another caller needs a non-'all' diagnosis value, or AdListParams is refactored
- touches: [src/features/ringkasan/AdList.tsx, src/api/ads.ts]

## D-20260917-17 differentiate from shopee
- context: owner approved the Shopee-style concept but rejected a near copy
- options: keep Shopee terms and columns | own metric set + per-ad verdicts + AI bridge | drop the table
- chosen: same concept, own content: cards lead with ROAS and Laba Iklan; table "Iklan Saya" with Biaya/Penjualan/ROAS/Laba/Penilaian and Tanya AI link; tabs Semua/Perlu Tindakan/Sehat; Sorotan card linking to /chat; every Shopee-only term (Tahap, Modal Harian, Target ROAS, Diagnosis, Tambah ke Keranjang, Proteksi ROAS, GMV Max) removed
- because: the product's value is judgment (verdicts, profit, AI), not re-displaying Seller Centre
- revisit-when: owner wants Shopee fields back for parity
- touches: [src/features/ringkasan/, src/api/ads.ts]
- supersedes: D-20260917-15 (content), keeps its structure

## D-20260917-18 table readability rules
- context: owner found the ad table too busy
- options: keep all deltas and inline reasons | one hero number per row with deltas only there, reasons in tooltip | card list instead of table
- chosen: one hero number (ROAS) with delta; other money compact and muted; verdict dot + word with tooltip reason; actions revealed on hover; filters in a single row with Selects instead of chip rows
- because: scanning speed; every extra number competes with the one decision the row supports
- revisit-when: owner wants deltas on Biaya/Penjualan back or needs touch-first actions
- touches: [src/features/ringkasan/AdList.tsx, future Iklan and Review Produk tables]
