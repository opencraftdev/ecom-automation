## D-20260916-1 tenancy model
- context: who uses the webapp
- options: single seller | agency multi-shop | SaaS multi-tenant
- chosen: SaaS multi-tenant
- because: owner wants to sell to many sellers; tenant_id + RLS from day one is cheaper than a retrofit
- revisit-when: never
- touches: [supabase/, src/api/, auth]

## D-20260916-2 backend
- context: Vite React needs a server for Shopee API and RAG
- options: Supabase | Cloudflare Workers + Vectorize | self-hosted Node/Express
- chosen: Supabase
- because: Postgres + pgvector + Auth + Edge Functions from one vendor; MCP already connected
- revisit-when: pgvector cost or Edge Function latency becomes a problem
- touches: [supabase/, src/api/]

## D-20260916-3 chat mode
- context: "chat with a marketer" could mean a human or an AI
- options: AI only (RAG) | human + AI copilot | two separate modes
- chosen: AI only (RAG over e-books + tenant ads data)
- because: owner confirmed; no inbox, presence, or human routing needed
- revisit-when: owner asks for human handoff
- touches: [src/features/chat, rag/]

## D-20260916-4 phase 1 scope
- context: no Shopee partner_id yet, registration in progress
- options: wait for API | build backend first | UI demo with mock data
- chosen: UI demo with mock data shaped like real Shopee responses, fetched through src/api/
- because: demoable now; swapping to Edge Functions later touches one layer
- revisit-when: partner_id + Ads API access granted
- touches: [src/mocks/, src/api/]

## D-20260916-5 skipped in phase 1
- context: YAGNI for the demo
- options: build auth+billing+RAG now | defer
- chosen: defer auth, billing, RAG to phases 2-3
- because: none of them is demoable without real data
- revisit-when: phase 1 complete
- touches: [auth, billing, rag/]

## D-20260916-6 language split
- context: owner's market is Indonesian sellers; agents and repo are English-first
- options: all Indonesian | all English | Indonesian UI, English code/docs/records
- chosen: Indonesian UI copy; English code, docs, logs, decisions
- because: users read the UI, agents read everything else
- revisit-when: never
- touches: [src/, docs/, CLAUDE.md]
